#include "main.h"
/**
 *add - adds two integers and returns a result
 *@a: int to be added to b
 *@b: int to be added to a
 * Return: result
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
