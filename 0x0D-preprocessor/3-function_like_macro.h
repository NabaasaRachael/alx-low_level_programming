#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H
/**
 * @x: number to check
 * ABS(x) - a macro that computes the absolute value of a number
 */
#define ABS(x) ((x) < 0 ? (-1) * (x) : (x))
#endif
